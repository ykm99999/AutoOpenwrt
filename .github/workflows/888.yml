name: SL3000-24.10源码-5.4内核-粉碎缓存版

on:
  repository_dispatch:
  workflow_dispatch:

env:
  REPO_URL: https://github.com/ykm99999/54
  REPO_BRANCH: 2410
  CONFIG_FILE: sl3000.config
  DIY_P2_SH: scripts/sl3000-patch.sh
  TZ: Asia/Shanghai

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
    - name: Checkout
      uses: actions/checkout@main

    - name: 物理环境初始化
      env:
        DEBIAN_FRONTEND: noninteractive
      run: |
        sudo rm -rf /etc/apt/sources.list.d/* /usr/share/dotnet /usr/local/lib/android /opt/ghc /opt/hostedtoolcache/CodeQL
        sudo -E apt-get -qq update
        sudo -E apt-get -qq install ack antlr3 asciidoc autoconf automake autopoint binutils bison build-essential bzip2 ccache cmake cpio curl device-tree-compiler fastjar flex gawk gettext gcc-multilib g++-multilib git gperf haveged help2man intltool libc6-dev-i386 libelf-dev libfuse-dev libglib2.0-dev libgmp3-dev libltdl-dev libmpc-dev libmpfr-dev libncurses5-dev libncursesw5-dev libpython3-dev libreadline-dev libssl-dev libtool lrzsz mkisofs msmtp ninja-build p7zip p7zip-full patch pkgconf python2.7 python3 python3-pyelftools python3-setuptools qemu-utils rsync scons squashfs-tools subversion swig texinfo uglifyjs upx-ucl unzip vim wget xmlto xxd zlib1g-dev
        sudo timedatectl set-timezone "$TZ"
        sudo mkdir -p /workdir
        sudo chown $USER:$GROUPS /workdir

    - name: 物理克隆仓库源 (ykm99999/54)
      working-directory: /workdir
      run: |
        git clone $REPO_URL -b $REPO_BRANCH openwrt
        ln -sf /workdir/openwrt $GITHUB_WORKSPACE/openwrt

    - name: 更新并安装 Feeds
      run: |
        cd openwrt
        ./scripts/feeds update -a
        ./scripts/feeds install -a

    - name: 加载自定义配置与脚本
      run: |
        [ -e $CONFIG_FILE ] && cp $CONFIG_FILE openwrt/.config
        chmod +x $DIY_P2_SH
        cd openwrt
        $GITHUB_WORKSPACE/$DIY_P2_SH

    - name: 下载源码 (粉碎旧记忆第一步)
      run: |
        cd openwrt
        # 物理粉碎：删除 tmp 目录，彻底清除旧 Makefile 索引
        rm -rf tmp
        # 重新生成干净的权威索引
        make defconfig
        make download -j8

    - name: 执行固件编译 (粉碎旧记忆第二步)
      run: |
        cd openwrt
        # 物理二次粉碎：确保在编译开始前不留任何旧缓存
        rm -rf tmp
        make defconfig
        
        # 优先编译工具链对齐物理路径
        make toolchain/install -j$(nproc) || make toolchain/install -j1 V=s
        
        # 物理编译 U-Boot (基于您的修复 Makefile 生成 FIP/DA)
        make package/boot/uboot-mediatek/compile V=s -j$(nproc)
        
        # 全局编译
        make -j$(nproc) || make -j1 V=s

    - name: 整理救砖固件产物
      id: organize
      run: |
        cd openwrt/bin/targets/*/*
        mkdir -p /tmp/COLLECT_DIR
        # 物理像素级匹配救砖核心件
        cp -f *DA* /tmp/COLLECT_DIR/ 2>/dev/null || true
        cp -f *fip* /tmp/COLLECT_DIR/ 2>/dev/null || true
        cp -f *u-boot* /tmp/COLLECT_DIR/ 2>/dev/null || true
        cp -f *sysupgrade.bin /tmp/COLLECT_DIR/ 2>/dev/null || true
        
        # 重新对齐上传目录
        rm -rf *
        cp -af /tmp/COLLECT_DIR/* . 2>/dev/null || true
        echo "FIRMWARE=$PWD" >> $GITHUB_ENV
        echo "status=success" >> $GITHUB_OUTPUT

    - name: 上传 SL3000 最终固件
      uses: actions/upload-artifact@main
      if: steps.organize.outputs.status == 'success'
      with:
        name: SL3000_Authority_Firmware
        path: ${{ env.FIRMWARE }}
